#include<stdio.h>
int main(){
    int n;
    printf("enter the number of times u want to print:");
    scanf("%d",&n);
    // for (initialization;condition;updation or increment)
    // { code }
    for (int i = 0; i <= n; i++)
    {
        printf("hello world\n");
    }
    
    return 0;
}